# Changelog

本文件记录项目的所有重要变更。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.1.0] - 2026-09-04

### 新增

- **批量推断进度与取消**：推断状态卡实时显示"已完成 X / Y 个基站"和确定性进度条，
  附"取消推断"按钮（单个/批量任务均可取消）；通知栏进度同步显示 (X/Y)。
  Worker 在相邻小区之间响应取消，已完成的中间结果不丢失
- **arXiv 论文**：撰写英文论文（LaTeX 源码 + PDF + 英文图表生成脚本，存于本地
  `paper/` 目录，不随仓库发布），覆盖信号模型、联合估计算法、解析梯度推导、
  仿真与实测验证
- **帮助页**：底部第 5 个 tab，内置必需权限说明、各品牌手机（vivo / 小米 / 华为 / OPPO）
  后台保活指引，并提供一键跳转"应用权限设置"和"电池优化设置"的按钮
- **地图信息框快捷推断**：点击测量点弹出的信息框中新增"▶ 推断该基站位置"入口，
  点击后自动跳转推断页并对该点所属小区（ECI）启动后台推断
- 仓库新增 `download/BsLocator-v1.0-debug.apk`，可直接下载安装
- 新增算法详解文档 `docs/算法详解.md`：信号模型、参数边界、Huber 损失、优化器、
  多起点策略、鲁棒性分析，附录 A 给出 8 个参数的解析梯度完整推导

### 修复

- **批量推断完成后报错**：基站数量较多时（如实测 112 个），结果 JSON 超出
  WorkManager Data 的 10KB 序列化上限导致任务被标记为失败；改为结果写入缓存文件、
  Data 仅传文件路径，读取后即删除

### 变更

- **方向图模型：3GPP 硬截断改为 tanh 平滑饱和**。原 3GPP min[raw, 30dB] 硬截断在
  截断区梯度恒为零，近基站高俯仰角采样点全部失去信息量；改为
  −30·tanh(raw/30)，主瓣区与 3GPP 完全一致、大角度平滑逼近同一 30dB 地板（实测
  旁瓣包络 28~30dB）且梯度处处非零。44 个真实小区 A/B 验证：总体无代价，含近距点
  的小区交叉验证 RMSE 明显改善（典型 6.68→4.53 dB）。技术报告与论文已同步说明
- **"清空推断基站"按钮无效**：地图页结果卡片上的"清空"/"清除选择"按钮的
  onClick 未绑定到点击事件，现修复为可点击按钮
- **重复点击"地图" tab 后采集点消失**：文件级全局变量记录已绘制数据，tab 切换
  重建 MapView 后误判"数据未变化"而跳过重绘；改为每个 MapView 实例独立的绘制状态
- **采集中地图点闪烁、渲染慢**：采集时每 0.5~2s 全量 clear 重绘数千个 Marker；
  改为增量渲染，仅追加新增点并更新轨迹线
- **推断扇区半径过大**：原算法为"最远测量点距离 × 1.5"，个别远距离点会把扇区
  画得远超实际覆盖；改为主瓣内测量点距离的 90 分位数 × 1.2，并钳制在 100m~2km

### 文档

- README（中英双语）补充安装与使用指南：权限说明、后台常驻设置、采集建议、
  导出方式；明确地图无需配置高德 Key
- 研究报告"未来工作"章节新增 IRLS 两轮拟合（近期待办）与 TA 距离辅助

## [1.0.0] - 2026-09-03

### 新增

- 首次发布：基站路测采集 + 天线方向图未知条件下的端上联合估计
- **采集**：前台 Service 记录 GPS + LTE/NR 服务小区信号（ECI/PCI、EARFCN、
  RSRP/RSRQ/SINR、CQI、TAC），Room 本地存储，GPS 精度过滤
- **推断**：8 参数联合优化（基站位置、方位角、波束宽度、下倾角、挂高、路损指数、
  参考电平），Huber 鲁棒损失 + 解析梯度 + Armijo 回溯 + 4 组多起点，
  WorkManager 后台执行，支持单小区与批量推断
- **地图**：高德地图叠加测量轨迹、按 ECI 着色/按 RSRP 调亮的测量点、推断基站
  星标与主瓣扇区，WGS-84 ↔ GCJ-02 坐标转换
- **日志**：采集会话管理、多选、CSV/JSON 导入导出（SAF）
- 离线分析脚本（`analysis/`）与 953 条真实路测数据（`data/`）
- 研究报告（`docs/`，Markdown + Word）
